En este programa, se declara un arreglo de objetos LocalDate que representa las fechas. Luego, se llama a la función obtenerFechaMasAntigua pasando el arreglo de fechas como argumento. La función recorre el arreglo y compara cada fecha con la fecha más antigua actual. Si encuentra una fecha anterior, actualiza la variable fechaMasAntigua. Al finalizar el recorrido, se devuelve la fecha más antigua encontrada.
